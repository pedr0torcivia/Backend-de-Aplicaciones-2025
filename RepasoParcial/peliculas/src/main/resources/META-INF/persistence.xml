<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

  <persistence-unit name="peliculas" transaction-type="RESOURCE_LOCAL">

    <!-- Entidades (@Entity) -->
    <class>blockbuster.entities.Genero</class>
    <class>blockbuster.entities.Director</class>
    <class>blockbuster.entities.Pelicula</class>

    <properties>
      <!-- ========= URL JDBC (ACÁ) ========= -->
      <!-- H2 en memoria + ejecuta TU script antes de la 1ª conexión -->
      <property name="jakarta.persistence.jdbc.url"
                value="jdbc:h2:mem:peliculas;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;INIT=RUNSCRIPT FROM 'classpath:META-INF/db-schema.sql'"/>

      <!-- Driver y credenciales -->
      <property name="jakarta.persistence.jdbc.driver" value="org.h2.Driver"/>
      <property name="jakarta.persistence.jdbc.user" value="sa"/>
      <property name="jakarta.persistence.jdbc.password" value=""/>

      <!-- Hibernate -->
      <property name="hibernate.dialect" value="org.hibernate.dialect.H2Dialect"/>
      <property name="hibernate.show_sql" value="true"/>
      <property name="hibernate.format_sql" value="true"/>

      <!-- No mezclar con generadores de esquema de JPA/Hibernate -->
      <property name="hibernate.hbm2ddl.auto" value="none"/>
    </properties>
  </persistence-unit>
</persistence>
